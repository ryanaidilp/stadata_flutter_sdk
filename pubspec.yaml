name: stadata_workspace
description: "A monorepo Flutter project with Melos for Stadata"
publish_to: none
version: 0.7.1

environment:
  sdk: ">=3.2.4 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  melos: ">=4.1.0 <7.0.0"
